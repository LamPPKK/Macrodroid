name: CI

on:
  push:
    branches: [master]
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    name: Validate Macrodroid
    runs-on: macos-15
    timeout-minutes: 45
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install validation tools
        run: |
          command -v jq >/dev/null || brew install jq
          command -v rg >/dev/null || brew install ripgrep
          command -v swiftlint >/dev/null || brew install swiftlint

      - name: Validate Macrodroid source, unsigned Release build, and native tests
        run: /bin/zsh scripts/verify-macrodroid.command

      - name: Reject whitespace and generated drift
        run: |
          git diff --check
          test -z "$(git status --short)"
